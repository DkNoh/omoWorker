<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.scbk.sms.mapper.employee.EmployeeQueryMapper">

    <select id="selectById" resultType="com.scbk.sms.vo.employee.EmployeeVO">
        /* EmployeeQueryMapper.selectById */
        SELECT
            E.EMP_ID AS empId,
            E.DEP_ID AS depId,
            E.EMP_NM AS empNm,
            E.EMP_LEV AS empLev,
            E.ACT_YN AS actYn
        FROM SMS.EMP E
        WHERE E.EMP_ID = #{empId}
          AND E.DEP_ID = #{depId}
    </select>

</mapper>
