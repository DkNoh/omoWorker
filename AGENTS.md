# 공통 작업 규칙

- 변경 전에 대상 파일, 직접 연결된 코드, 대표 패턴 구현 1~2개와 직접 관련된 테스트만 확인한다.
- 기존 코드 스타일과 구조를 따르되, 패턴 조사를 위해 저장소 전체를 불필요하게 탐색하지 않는다.
- 요청 범위를 벗어난 리팩터링과 기존 결함 수정은 하지 않는다.
- 테스트는 가장 좁은 관련 테스트부터 실행한다.
- 전체 테스트·린트·빌드는 변경이 공통 코드에 영향을 주거나 사용자가 명시적으로 요구한 경우에만 실행한다.
- UI 변경은 수정한 화면과 동작만 브라우저에서 확인한다.
- 검증 중 발견된 문제는 이번 변경으로 발생한 회귀만 수정한다.
- 관련 검증이 통과하면 추가 탐색이나 개선 없이 작업을 종료한다.
- 비밀값, API 키, 토큰, 인증정보 및 .env 내용을 출력하거나 커밋하지 않는다.
- 요구사항의 모호함이 결과를 크게 바꾸지 않으면 가장 작은 범위로 해석한다.
- 모호함에 따라 구현 방향이 크게 달라질 때만 사용자에게 확인한다.
- 로컬 Oracle은 `oracleup`으로만 기동한다. 컨테이너 이름을 직접 `docker start`하거나 별도 Oracle 컨테이너·이미지를 만들지 않는다.
- Oracle 데이터는 외부 볼륨 `smstest-v3_sms-oracle-data`가 단일 기준이다. 이 볼륨을 삭제·초기화·교체하지 않는다.
- Spring 로컬 실행은 `springup`을 사용한다. Oracle 관리자는 `SYSTEM/oracle`, 앱 계정은 `SMS/1234`, 서비스는 `localhost:1521/SMS`다.
