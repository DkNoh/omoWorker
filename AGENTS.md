# SMS V3 프로젝트 작업 규칙

이 파일은 OpenCode, Codex 및 기타 코딩 에이전트가 이 저장소에서 가장 먼저 읽는 프로젝트 진입 문서다.

## 프로젝트

- Spring Boot 3.3, Java 21, Maven, MyBatis, Oracle 기반 프로젝트다.
- 사용자와 부서는 운영에서 전달받은 `EMP`, `DEP`를 기준으로 한다.
- 인증 방식은 local/dev/prod마다 다르지만 권한 판단은 공통 v3 권한 테이블을 사용한다.

## 규칙 로딩 순서

1. `docs/base/rules-index.md`를 규칙 라우팅의 단일 기준으로 읽는다.
2. `docs/base/README.md`에서 프로젝트의 전체 방향을 확인한다.
3. `docs/base/rules-index.md`의 현재 작업 유형에 지정된 문서만 읽는다.
4. 작업 유형에 해당하는 `.claude/rules/*.md` 실행 규칙을 함께 적용한다.

작업과 무관한 규칙 문서를 추측해서 모두 읽지 않는다. 한 작업에서 읽는 `docs/base` 문서는 4개를 넘기지 않고, 초과하면 작업을 더 작게 나눈다.

## 필수 정책

- 운영(prod) 서버 또는 운영 DB 작업은 사전 승인 없이 진행하지 않는다.
- 지시받지 않은 패턴이나 라이브러리를 임의로 도입하지 않는다.
- 기존 코드 구조와 프로젝트 규약을 우선하며 관련 없는 파일을 수정하지 않는다.
- 사용자/부서는 `EMP`, `DEP`, `EMP_ID + DEP_ID` 식별 기준으로 설계한다.
- `EMP.PERM_*`는 v3 권한 판단에 사용하지 않는다.
- 메뉴 테이블 변경 시 DBA 전달용 DDL 문서와 seed 문서를 함께 관리한다.
- 메뉴 source 정책은 `docs/base/menu-source-policy.md`를 따른다.
- 문서와 코드가 충돌하면 임의로 선택하지 말고 충돌 내용을 보고한다.
- 비밀번호, 토큰, 개인정보를 출력하거나 저장소에 커밋하지 않는다.

## 작업 방식

1. 요구사항을 기존 코드와 해당 정책 문서에 대조한다.
2. 불명확한 업무 정책은 임의 구현하지 않고 미확정 사항으로 기록한다.
3. 기존 구현을 확인한 뒤 필요한 범위만 변경한다.
4. 변경 후 작업 유형에 맞는 검증 명령을 실행한다.
5. 검증 실패 시 원인을 수정하고 같은 검증을 반복한다.
6. 같은 오류가 3회 반복되면 오류 원문, 시도한 방법 3가지, 추정 원인을 보고한다.

## 검증

코드 변경 후 다음 명령을 모두 통과해야 한다.

```powershell
.\mvnw.cmd test
.\mvnw.cmd -DskipTests package
```

문서나 설정만 변경하고 코드 동작에 영향을 주지 않는 작업은 Maven 검증 대상이 아니다.
