# omoWorker 진행 상황 체크리스트

기준일: 2026-06-27
omoWorker 20 커밋, 122/122 테스트 통과

---

## 체크리스트 (사용자 요청 #1-6)

- [x] **#1 세션 만료 시 화면 전환** — axios 응답 인터셉터에서 로그인 HTML 감지 → `location.replace('/login')`. tui-page-builder의 임시 처리 제거, 인터셉터로 중앙화
- [x] **#2 SQL 로깅 (p6spy)** — local/dev만 적용(prod JNDI 제외). P6SpyFormatter + spyProperties. `jdbc:p6spy:oracle:thin:` URL prefix
- [x] **#3 쿼리 시그니처** — `/* Mapper.method */` 주입. 스캐폴드 엔진(MapperXmlTemplate) + 기존 mapper 8개 파일 19문 전수 retrofit
- [x] **#4 POJO + Lombok 혼재** — 순수 데이터 DTO/VO 7종 Lombok `@Data` 전환. 규칙 위치기반→타입기반 정정(project.md)
- [x] **#5 코딩 컨벤션** — spotless는 기존 pom에 있음. `.editorconfig`는 미작성(별도 검토). mybatis-oracle SELECT* 규칙 현행화(파생테이블 A.* 허용)
- [x] **#6 스캐폴드 테스트** — searchConditions 공통화(이미 구현됨 확인). validate=required → @NotBlank/@NotNull 서버 검증 생성 추가

---

## Oracle 로드맵

### A-1. Scaffold Contract (계약)
- [x] **§2 소유권 마커** — 12개 템플릿 전부에 `Scaffold 생성(v1) — scaffold 소유` 헤더 부착
- [x] **§3 재생성 보존** — ScaffoldFileApplier가 마커 없는 기존 파일을 USER_OWNED로 간주해 덮어쓰지 않음
- [x] **§4-B Rules + hook** — RulesTemplate가 `{Domain}Rules` @Component 생성(validateOnCreate/Update stub). ServiceTemplate가 create/update에서 hook 호출. Rules는 마커 없음→재생성 보존
- [x] **scaffold-contract.md** — 현행/목표 분리(§0 상태 테이블). 미구현을 구현된 것처럼 적었던 거짓 시정

### A-2. 수직 슬라이스 (SmsHistory 패턴 선언화)
- [x] **마스킹** — includePrivacy + maskType → MaskingUtil.maskPhone/maskName/maskRrn/maskCard 실제 호출 생성(목록·엑셀). TODO 주석 제거
- [x] **검증** — validate=required → @NotBlank(String) / @NotNull(비String) 서버 어노테이션 생성
- [x] **원문 상세 (/unmask)** — ControllerTemplate: @PrivacyLog @GetMapping("/unmask"). ServiceTemplate: getUnmaskedDetail(pk) + DATA_NOT_FOUND. MapperXml: selectDetail(baseQuery + PK WHERE). MapperInterface: selectDetail 메서드. MenuAuthInterceptor가 /unmask에 MASK_VIEW 권한 자동 적용
- [x] **엑셀** — (기존 구현 확인) includeExcel → downloadExcel + selectListForExcel + 마스킹 연동

### A-3. 드리프트 게이트
- [x] **golden hash 테스트** — 13개 템플릿 출력의 hashCode를 golden 값(556655004)과 비교. 출력 변경 시 테스트 실패 → 의도적이면 golden 갱신

### A-4. 확장
- [x] **#1 메뉴 권한 최소화** — MenuSqlTemplate가 screenMode별 CAN_* 기본값 생성(LIST→READ, EXCEL→READ+DOWNLOAD, CRUD→CRUD 4종, privacy→+MASK_VIEW)
- [x] **#5 세션 캐싱 (메뉴 트리)** — GlobalModelAdvice가 HttpSession에 menuTree 캐싱
- [x] **#5 세션 캐싱 (per-URL 권한)** — resolvePageAuth가 URL별 PageAuth를 세션 Map에 캐싱
- [x] **#7 UNION/WITH 검증** — JSQLParser가 UNION/CTE 쿼리에서 컬럼 정상 추출함을 테스트로 확인
- [x] **(기존) PK 자동 추론** — ScaffoldMetadataReader가 DB 메타데이터에서 PK 자동 감지
- [ ] **#4 메뉴관리화면** — TB_MENU/TB_MENU_AUTH CRUD 관리 화면. 요구사항 정의 필요(트리구조? 권한매트릭스?)
- [ ] **#5 API 요청 모델 스킵** — 한계수익(세션 캐시로 DB 조회 이미 제거됨, 남은 건 μs 단위 HashMap 조회)
- [ ] **#7 추가 SQL 샘플** — CASE/Oracle 함수 등 추가 케이스. 한계수익
- [ ] **#8 폐쇄망 패키지 갱신** — 운영 작업(zip/base64 재생성)

---

## 엔진 개선
- [x] **baseQuery DRY** — MapperXmlTemplate가 `<sql id="baseQuery">` 1회 정의 후 `<include>` 참조. rawQuery 3회 인라인→1회
- [x] **쿼리 시그니처 자동 주입** — MapperXmlTemplate 각 select에 `/* {Domain}Mapper.{method} */` 생성

---

## 문서 감사 (28개, 불일치 12건 정정)
- [x] common-response-contract.md — @RestControllerAdvice→@ControllerAdvice, @Valid 전체에러, 팩토리 오버로드
- [x] screen-convention.md — gridDefaults bodyHeight420+scrollY, 세션만료 /login 전환
- [x] rest-api-standard.md — /api/** 보조API 명시
- [x] v2-scaffold-reference.md — jsqlparser com.github, ExcelUtil 존재
- [x] query-scaffold-implementation.md — 템플릿 12종
- [x] test-automation-guide.md — 산출물 수 현행화
- [x] mybatis-oracle.md — SELECT A.* 허용(파생테이블)
- [x] testing.md — @MybatisTest 미사용 현실 명시
- [x] project.md — Lombok 규칙 타입기반 정정
- [x] scaffold-contract.md — 현행/목표 분리
- [x] remaining-work.md — #1/#2/#5/#6/#7 해결 표시
- [ ] deployment-checklist.md — JBoss vs Tomcat 확인 필요 (사용자 환경)

---

## 기타 완료
- [x] **세션 만료 화면 전환** — common-utils.js 응답 인터셉터에 세션 만료 감지(text/html 응답) → location.replace('/login')
- [x] **p6spy SQL 로깅** — local/dev 전용. prod(JNDI)는 미사용
- [x] **발송이력조회 화면 복구** — TUI Grid #grid 빈 mount 복구 + 빈 상태 sibling overlay
- [x] **발송이력조회 디자인** — 필터 툴바 재정돈, 한국어 라벨, 빈 상태 개선, 세로 스크롤, 그리드 디자인 개선
- [x] **CoreUI 디자인 토큰** — DESIGN.md + admin-common.css 83개 토큰. CoreUI 정돈
- [x] **POJO→Lombok 전환** — 순수 데이터 DTO/VO 7종

---

## 커밋 히스토리 (20건)

| # | 해시 | 메시지 |
|---|---|---|
| 1 | d439065 | chore: omoWorker 초기 기준선 |
| 2 | 8b152ad | feat(config): p6spy SQL 로깅(local/dev, prod 제외) |
| 3 | 2754b02 | feat(mapper): 쿼리 시그니처 주입 - 엔진+기존 mapper |
| 4 | e90ae31 | refactor(scaffold): baseQuery DRY |
| 5 | c47d447 | docs: 감사 정정 10건 + scaffold-contract v1 |
| 6 | 9b335b5 | docs(rules): mybatis SELECT* / testing @MybatisTest 현행화 |
| 7 | e42afb2 | feat(scaffold): 소유권 마커(Service/Controller/MapperXml) |
| 8 | 013f5b7 | feat(scaffold): 소유권 마커 전 템플릿(12/12) |
| 9 | acac77b | feat(scaffold): 재생성 보존 로직(USER_OWNED) |
| 10 | cfdd33d | feat(scaffold): includePrivacy 실제 마스킹 생성 |
| 11 | 69356e5 | feat(scaffold): validate=required→@NotBlank/@NotNull |
| 12 | 3c21b9f | feat(scaffold): Rules 확장 클래스 + hook(A-1 §4-B) |
| 13 | c5ca2bd | docs: test-automation-guide 산출물 수 현행화 |
| 14 | 8898892 | feat(scaffold): /unmask 원문 상세 endpoint(A-2 완료) |
| 15 | 71da46f | test(scaffold): 드리프트 게이트 golden hash(A-3) |
| 16 | 864275d | feat(scaffold): menu 권한 최소화(A-4 #1) |
| 17 | 96a74a0 | docs: remaining-work 현행화 |
| 18 | e61a771 | test(scaffold): UNION/WITH 검증(A-4 #7) |
| 19 | d52ce21 | perf: 메뉴 트리 세션 캐싱(A-4 #5) |
| 20 | 79da37f | perf: per-URL 권한 세션 캐싱(A-4 #5) |
