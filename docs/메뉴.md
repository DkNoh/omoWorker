# 역할·메뉴 권한 운영 가이드

## 결론

사용자별 직접 권한이 아니라 **역할(Role) 기반 권한**으로 설계한다.

현재 운영 기준 테이블은 `EMP`, `DEP`만 존재하므로, v3 메뉴/권한 관리를 위해 다음 4개 테이블을 추가한다.

```text
TB_ROLE       역할
TB_EMP_ROLE   직원(EMP_ID + DEP_ID) ↔ 역할 매핑
TB_MENU       메뉴 트리 / URL
TB_MENU_AUTH  역할별 메뉴 기능 권한
```

`EMP.PERM_*`는 v3 권한 판단에 사용하지 않는다. 사용자는 항상 `(EMP_ID, DEP_ID)`로 식별한다.

## 구현 현황

2026-08-01 현재 구현과 계획을 구분한다.

| 영역 | 상태 | 현재 기준 |
|---|---|---|
| 역할·메뉴·권한 테이블과 DB source | 구현 | `TB_ROLE`, `TB_EMP_ROLE`, `TB_MENU`, `TB_MENU_AUTH`, `DbMenuSource` |
| URL/API 권한 차단 | 구현 | `MenuAuthInterceptor`가 메뉴 URL과 suffix별 필요 권한을 매 요청 검증 |
| LNB·화면 버튼 권한 | 구현 | `GlobalModelAdvice`가 역할별 메뉴와 `pageAuth`를 제공 |
| 메뉴 관리 | 구현 | `/system/menu-manage`에서 메뉴 트리, 메뉴 CRUD, 활성 역할별 8개 권한 매트릭스를 함께 관리 |
| 메뉴·권한 변경 후 세션 갱신 | 구현 | DB 커밋 후 메뉴 revision을 올리고, 다음 화면 요청에서 해당 세션의 LNB와 `pageAuth` 캐시를 함께 재조회 |
| 역할 관리 | 미구현 | `/system/role-manage` 전용 화면과 역할 비활성화 흐름 필요 |
| 사용자 역할 관리 | 미구현 | `/system/emp-role-manage` 전용 화면과 `(EMP_ID, DEP_ID)` 기준 역할 부여 흐름 필요 |
| 독립 권한 관리 화면 | 선택 사항 | 현재 권한 매트릭스는 `/system/menu-manage`에 포함되어 있어 `/system/auth-manage`는 운영 UX상 필요할 때 분리 |

현재 메뉴에 등록된 업무 화면은 테스트·참고 화면이므로 운영 화면 마이그레이션 완료 여부를 판단하는 기준으로 사용하지 않는다. 이 문서의 구현 상태는 권한 인프라와 관리 기능 자체를 뜻한다.

## 권한 단위

권한은 화면 메뉴 단위로 나누고, URL suffix 규칙으로 API 접근까지 검증한다.

이 문서의 권한은 **화면/메뉴 접근 권한**이다. 승인 업무의 결재선, 승인 요청 상태, 승인 이력은 별도 승인 시스템에서 관리한다. 예를 들어 `CAN_APPROVE='Y'`는 `/approve` 버튼과 API를 호출할 수 있다는 뜻이지, 특정 건의 승인 순번/대리승인/반려 사유까지 보장한다는 뜻이 아니다. 그 업무 로직은 `docs/승인.md`의 승인 테이블과 Service 트랜잭션으로 검증한다.

| 권한 | 의미 |
|---|---|
| `CAN_READ` | 메뉴 노출, 화면 접근, 목록/상세 조회 |
| `CAN_CREATE` | 등록 |
| `CAN_UPDATE` | 수정 |
| `CAN_DELETE` | 삭제 |
| `CAN_APPROVE` | 승인/반려 |
| `CAN_CANCEL` | 취소성 업무 |
| `CAN_DOWNLOAD` | 엑셀/파일 다운로드 |
| `CAN_MASK_VIEW` | 개인정보 원문/비마스킹 조회 |

`CAN_READ`는 기본 전제 권한이다. 관리 화면에서는 `CREATE/UPDATE/DELETE/APPROVE/CANCEL/DOWNLOAD/MASK_VIEW` 중 하나를 켜면 `READ`도 자동으로 켜지게 한다.

## 관리 화면 구성

한 화면에 모든 기능을 넣지 말고 다음 화면 또는 탭으로 분리한다.

### 1. 메뉴 트리 관리

`TB_MENU`를 관리한다.

- 메뉴명
- 메뉴 ID
- 상위 메뉴
- URL
- 정렬순서
- 메뉴 타입: 그룹(`G`) / 화면 메뉴(`M`)
- 표시 여부(`DISPLAY_YN`)
- 사용 여부(`USE_YN`)
- 시스템 보호 여부(`SYSTEM_YN`)

규칙:

- 그룹 메뉴(`G`)는 URL이 없다.
- 화면 메뉴(`M`)는 URL이 필수다.
- 좌측 메뉴에는 `DISPLAY_YN='Y'`, `USE_YN='Y'`, `CAN_READ='Y'`인 화면 메뉴만 노출한다.
- 그룹 메뉴는 직접 권한을 부여하기보다, 읽을 수 있는 자식 메뉴가 있으면 자동 노출하는 방식이 좋다.
- `SYSTEM_YN='Y'`인 메뉴는 삭제하거나 URL을 바꾸지 못하게 막는다.

### 2. 역할 관리

`TB_ROLE`을 관리한다.

- 역할 코드: `ROLE_ADMIN`, `ROLE_MANAGER`, `ROLE_USER`, `ROLE_VIEWER` 등
- 역할명
- 설명
- 정렬순서
- 사용 여부

역할은 물리 삭제보다 `USE_YN='N'` 처리로 비활성화한다.

### 3. 역할별 메뉴 권한 매트릭스

`TB_MENU_AUTH`를 관리한다.

권장 UI:

```text
역할 선택: [ROLE_ADMIN v]

메뉴명              READ CREATE UPDATE DELETE APPROVE CANCEL DOWNLOAD MASK_VIEW
기본메뉴            [ ]  [ ]    [ ]    [ ]    [ ]     [ ]    [ ]      [ ]
  공지사항          [x]  [x]    [x]    [x]    [ ]     [ ]    [ ]      [ ]
  발송이력조회      [x]  [ ]    [ ]    [ ]    [ ]     [ ]    [x]      [x]
```

저장 시 `(MENU_ID, ROLE_CD)` 기준으로 `TB_MENU_AUTH`를 upsert한다.

권장 동작:

- `READ`를 끄면 나머지 권한도 함께 끈다.
- `READ`가 꺼진 메뉴는 좌측 메뉴에 노출하지 않는다.
- `DOWNLOAD`는 `/excel`, `/download`, `/export` 접근 권한이다.
- `MASK_VIEW`는 `/unmask` 또는 개인정보 원문 조회 권한이다.
- `APPROVE`, `CANCEL`은 승인/취소 업무가 있는 화면에서만 사용한다.
- `APPROVE`는 메뉴 권한일 뿐이다. 실제 승인 가능 여부는 승인 요청의 현재 단계, 승인자, 상태값을 승인 시스템 테이블에서 다시 판단한다.

### 4. 사용자 역할 부여

`TB_EMP_ROLE`을 관리한다.

흐름:

```text
EMP + DEP 조회 → 사용자 선택 → 역할 체크 → TB_EMP_ROLE 저장
```

권장 UI:

- 사번/이름/부서 검색
- 사용자 선택
- 역할 목록 체크박스
- 사용 여부

사용자에게 메뉴 권한을 직접 부여하지 않는다. 직접 권한을 열면 권한 추적이 어려워진다.

## 복수 역할 권한 합성 규칙

한 사용자는 여러 역할을 가질 수 있다. 이때 권한은 역할별 플래그의 **허용 우선 OR 합성**이다.

- `MenuAuthMapper.selectMenuPermissions`는 각 `CAN_*` 값을 `MAX`로 집계한다. 역할 중 하나라도 `Y`이면 최종 권한은 허용된다.
- LNB는 역할 중 하나라도 `CAN_READ='Y'`인 메뉴를 `DISTINCT`로 노출한다.
- 별도의 `DENY` 또는 약한 역할 우선 규칙은 없다. `ROLE_VIEWER`를 추가해도 `ROLE_MANAGER`가 가진 쓰기 권한은 줄어들지 않는다.
- 권한을 줄이려면 강한 역할을 제거하거나, 부여된 모든 역할의 해당 메뉴 권한을 `N`으로 바꿔야 한다.
- 로그인 principal의 역할 목록은 로그인 시 확정된다. `TB_EMP_ROLE`을 변경한 사용자는 다시 로그인해야 새 역할 목록이 반영된다. 메뉴/권한 플래그 변경은 메뉴 revision으로 다음 화면 요청에 반영된다.

## AS-IS `EMP.PERM_*` 이관 기준

`EMP.PERM_*`는 TO-BE 런타임 권한으로 직접 읽지 않는다. 초기 이관 때 역할과 메뉴 권한을 설계하기 위한 참고값으로만 사용하며, 다음 표는 자동 변환 규칙이 아니라 검토 라우팅이다.

| AS-IS 필드 | 해석 후보 | TO-BE 검토 대상 | 자동 이관 |
|---|---|---|---|
| `PERM_CPN` | 캠페인 업무 범위 | 캠페인 메뉴의 역할별 `CAN_READ/CREATE/UPDATE/DELETE` | 금지 |
| `PERM_SYS` | 시스템 관리 범위 | 시스템 메뉴와 `ROLE_ADMIN` 계열 역할 | 금지 |
| `PERM_STA` | 통계 업무 범위 | 통계 메뉴의 `CAN_READ`, 필요 시 `CAN_DOWNLOAD` | 금지 |
| `PERM_PSN` | 개인정보 업무 범위 | 개인정보 메뉴 접근과 `CAN_MASK_VIEW`를 분리해 검토 | 금지 |
| `PERM_CPN_AGREE`, `PERM_NOM_AGREE`, `PERM_MMS_AGREE` | 업무별 승인 후보 | 해당 메뉴의 `CAN_APPROVE`와 별도 승인선 데이터 | 금지 |
| `PERM_SND_CANCEL` | 발송 취소 후보 | 해당 메뉴의 `CAN_CANCEL` | 금지 |
| `PERM_AUT`, `PERM_NOM`, `PERM_MMS` | legacy 업무 범위가 현재 문서만으로 확정되지 않음 | 원 시스템 정의와 실제 메뉴를 확인한 뒤 역할·메뉴에 매핑 | 금지 |

이관 절차는 `EMP.PERM_* → 역할 후보 산정 → 업무 담당자 확인 → TB_EMP_ROLE/TB_MENU_AUTH 적재` 순서다. 승인 계열 필드가 `Y`여도 특정 승인 건을 처리할 수 있다는 뜻으로 옮기지 않는다.

## URL suffix 권한 매핑

현재 v3 권한 검증은 메뉴 URL과 요청 URL suffix를 기준으로 판단한다.

| URL 패턴 | 필요 권한 |
|---|---|
| 화면 URL, `/data`, `/search`, `/detail`, `/tree` | `CAN_READ` |
| `/create`, `/register` | `CAN_CREATE` |
| `/update` | `CAN_UPDATE` |
| `/delete` | `CAN_DELETE` |
| `/approve`, `/reject` | `CAN_APPROVE` |
| `/cancel` | `CAN_CANCEL` |
| `/excel`, `/download`, `/export` | `CAN_DOWNLOAD` |
| `/unmask` | `CAN_MASK_VIEW` |

Controller에서 권한을 중복 검사하지 않는다. 실제 URL/API 차단은 `MenuAuthInterceptor`가 담당한다.

단, 승인 업무 Service는 별도 업무 검증을 수행한다. `MenuAuthInterceptor`는 “이 사용자가 승인 API에 접근 가능한 역할인가”만 판단하고, “이 사용자가 이 승인 건의 현재 승인자인가”는 승인 Service가 판단한다.

## 남은 구현 순서

1. 역할별 운영 권한 매트릭스와 AS-IS 이관값을 업무 담당자와 확정
2. 사용자 역할 관리 화면을 구현하고 `(EMP_ID, DEP_ID)` 복합 식별을 검증
3. 역할 관리 화면을 구현하고 사용 중 역할의 비활성화 정책을 확정
4. dev/prod에서 `sms.menu.source=db`, `sms.role.source=db` 역할별 시나리오를 검증
5. 독립 `/system/auth-manage` 화면은 메뉴 관리 내 매트릭스로 운영하기 어려울 때만 분리

## 테이블 설계 및 스크립트

전제:

- Schema: `SMS`
- Oracle 19c 기준
- 기존 사용자/부서 테이블: `SMS.EMP`, `SMS.DEP`
- 사용자 식별 키: `(EMP_ID, DEP_ID)`
- 운영 반영 DDL에는 `DROP TABLE`을 넣지 않는다.

### 1. `TB_ROLE`

역할 코드 테이블이다.

```sql
CREATE TABLE SMS.TB_ROLE (
    ROLE_CD    VARCHAR2(30)  NOT NULL,
    ROLE_NM    VARCHAR2(100) NOT NULL,
    ROLE_DESC  VARCHAR2(500),
    SORT_ORD   NUMBER(5)     DEFAULT 0 NOT NULL,
    USE_YN     CHAR(1)       DEFAULT 'Y' NOT NULL,
    REG_ID     VARCHAR2(12)  NOT NULL,
    REG_DTTM   TIMESTAMP(6)  DEFAULT SYSTIMESTAMP NOT NULL,
    UPD_ID     VARCHAR2(12),
    UPD_DTTM   TIMESTAMP(6),
    CONSTRAINT PK_TB_ROLE PRIMARY KEY (ROLE_CD),
    CONSTRAINT CK_TB_ROLE_USE_YN CHECK (USE_YN IN ('Y', 'N'))
);

COMMENT ON TABLE SMS.TB_ROLE IS 'v3 역할 코드';
COMMENT ON COLUMN SMS.TB_ROLE.ROLE_CD IS '역할 코드. Spring Security authority와 동일하게 사용';
COMMENT ON COLUMN SMS.TB_ROLE.ROLE_NM IS '역할명';
COMMENT ON COLUMN SMS.TB_ROLE.ROLE_DESC IS '역할 설명';
COMMENT ON COLUMN SMS.TB_ROLE.SORT_ORD IS '정렬순서';
COMMENT ON COLUMN SMS.TB_ROLE.USE_YN IS '사용 여부';
```

기본 역할 seed:

```sql
INSERT INTO SMS.TB_ROLE (ROLE_CD, ROLE_NM, ROLE_DESC, SORT_ORD, USE_YN, REG_ID)
VALUES ('ROLE_ADMIN', '시스템 관리자', '메뉴/권한/사용자/시스템 관리 권한', 10, 'Y', 'SYSTEM');

INSERT INTO SMS.TB_ROLE (ROLE_CD, ROLE_NM, ROLE_DESC, SORT_ORD, USE_YN, REG_ID)
VALUES ('ROLE_MANAGER', '업무 관리자', '업무 조회/등록/수정/승인 권한', 20, 'Y', 'SYSTEM');

INSERT INTO SMS.TB_ROLE (ROLE_CD, ROLE_NM, ROLE_DESC, SORT_ORD, USE_YN, REG_ID)
VALUES ('ROLE_USER', '일반 사용자', '일반 업무 조회 및 제한적 등록 권한', 30, 'Y', 'SYSTEM');

INSERT INTO SMS.TB_ROLE (ROLE_CD, ROLE_NM, ROLE_DESC, SORT_ORD, USE_YN, REG_ID)
VALUES ('ROLE_VIEWER', '조회 사용자', '조회 전용 권한', 40, 'Y', 'SYSTEM');
```

### 2. `TB_EMP_ROLE`

직원과 역할의 매핑 테이블이다. 권한은 직원에게 직접 주지 않고 역할에 부여한다.

```sql
CREATE TABLE SMS.TB_EMP_ROLE (
    EMP_ID    VARCHAR2(12) NOT NULL,
    DEP_ID    VARCHAR2(12) NOT NULL,
    ROLE_CD   VARCHAR2(30) NOT NULL,
    USE_YN    CHAR(1)      DEFAULT 'Y' NOT NULL,
    REG_ID    VARCHAR2(12) NOT NULL,
    REG_DTTM  TIMESTAMP(6) DEFAULT SYSTIMESTAMP NOT NULL,
    UPD_ID    VARCHAR2(12),
    UPD_DTTM  TIMESTAMP(6),
    CONSTRAINT PK_TB_EMP_ROLE PRIMARY KEY (EMP_ID, DEP_ID, ROLE_CD),
    CONSTRAINT FK_TB_EMP_ROLE_EMP FOREIGN KEY (EMP_ID, DEP_ID)
        REFERENCES SMS.EMP (EMP_ID, DEP_ID),
    CONSTRAINT FK_TB_EMP_ROLE_ROLE FOREIGN KEY (ROLE_CD)
        REFERENCES SMS.TB_ROLE (ROLE_CD),
    CONSTRAINT CK_TB_EMP_ROLE_USE_YN CHECK (USE_YN IN ('Y', 'N'))
);

CREATE INDEX SMS.IX_TB_EMP_ROLE_01 ON SMS.TB_EMP_ROLE (ROLE_CD);
CREATE INDEX SMS.IX_TB_EMP_ROLE_02 ON SMS.TB_EMP_ROLE (DEP_ID, EMP_ID);

COMMENT ON TABLE SMS.TB_EMP_ROLE IS 'v3 직원-역할 매핑';
COMMENT ON COLUMN SMS.TB_EMP_ROLE.EMP_ID IS '직원 ID. EMP.EMP_ID';
COMMENT ON COLUMN SMS.TB_EMP_ROLE.DEP_ID IS '부서 ID. EMP.DEP_ID';
COMMENT ON COLUMN SMS.TB_EMP_ROLE.ROLE_CD IS '역할 코드. TB_ROLE.ROLE_CD';
```

관리자 역할 부여 예시:

```sql
MERGE INTO SMS.TB_EMP_ROLE T
USING (
    SELECT '관리자사번' EMP_ID, '관리자부서' DEP_ID, 'ROLE_ADMIN' ROLE_CD FROM DUAL
) S
ON (T.EMP_ID = S.EMP_ID AND T.DEP_ID = S.DEP_ID AND T.ROLE_CD = S.ROLE_CD)
WHEN MATCHED THEN UPDATE SET
    T.USE_YN = 'Y',
    T.UPD_ID = 'SYSTEM',
    T.UPD_DTTM = SYSTIMESTAMP
WHEN NOT MATCHED THEN INSERT (
    EMP_ID, DEP_ID, ROLE_CD, USE_YN, REG_ID
) VALUES (
    S.EMP_ID, S.DEP_ID, S.ROLE_CD, 'Y', 'SYSTEM'
);
```

### 3. `TB_MENU`

메뉴 트리와 화면 URL을 관리한다.

```sql
CREATE TABLE SMS.TB_MENU (
    MENU_ID         VARCHAR2(50)  NOT NULL,
    PARENT_MENU_ID  VARCHAR2(50),
    MENU_NM         VARCHAR2(100) NOT NULL,
    MENU_URL        VARCHAR2(300),
    MENU_LEVEL      NUMBER(2)     NOT NULL,
    SORT_ORD        NUMBER(5)     DEFAULT 0 NOT NULL,
    MENU_TYPE       CHAR(1)       DEFAULT 'M' NOT NULL,
    ICON_NM         VARCHAR2(50),
    DISPLAY_YN      CHAR(1)       DEFAULT 'Y' NOT NULL,
    USE_YN          CHAR(1)       DEFAULT 'Y' NOT NULL,
    SYSTEM_YN       CHAR(1)       DEFAULT 'N' NOT NULL,
    REMARK          VARCHAR2(500),
    REG_ID          VARCHAR2(12)  NOT NULL,
    REG_DTTM        TIMESTAMP(6)  DEFAULT SYSTIMESTAMP NOT NULL,
    UPD_ID          VARCHAR2(12),
    UPD_DTTM        TIMESTAMP(6),
    CONSTRAINT PK_TB_MENU PRIMARY KEY (MENU_ID),
    CONSTRAINT FK_TB_MENU_PARENT FOREIGN KEY (PARENT_MENU_ID)
        REFERENCES SMS.TB_MENU (MENU_ID),
    CONSTRAINT CK_TB_MENU_TYPE CHECK (MENU_TYPE IN ('G', 'M')),
    CONSTRAINT CK_TB_MENU_DISPLAY_YN CHECK (DISPLAY_YN IN ('Y', 'N')),
    CONSTRAINT CK_TB_MENU_USE_YN CHECK (USE_YN IN ('Y', 'N')),
    CONSTRAINT CK_TB_MENU_SYSTEM_YN CHECK (SYSTEM_YN IN ('Y', 'N')),
    CONSTRAINT CK_TB_MENU_URL_BY_TYPE CHECK (
        (MENU_TYPE = 'G' AND MENU_URL IS NULL)
        OR (MENU_TYPE = 'M' AND MENU_URL IS NOT NULL)
    )
);

CREATE UNIQUE INDEX SMS.UX_TB_MENU_URL ON SMS.TB_MENU (MENU_URL);
CREATE INDEX SMS.IX_TB_MENU_01 ON SMS.TB_MENU (PARENT_MENU_ID, SORT_ORD);
CREATE INDEX SMS.IX_TB_MENU_02 ON SMS.TB_MENU (USE_YN, DISPLAY_YN);

COMMENT ON TABLE SMS.TB_MENU IS 'v3 메뉴 트리';
COMMENT ON COLUMN SMS.TB_MENU.MENU_ID IS '메뉴 ID';
COMMENT ON COLUMN SMS.TB_MENU.PARENT_MENU_ID IS '상위 메뉴 ID';
COMMENT ON COLUMN SMS.TB_MENU.MENU_NM IS '메뉴명';
COMMENT ON COLUMN SMS.TB_MENU.MENU_URL IS '화면 URL';
COMMENT ON COLUMN SMS.TB_MENU.MENU_TYPE IS 'G: 그룹, M: 화면 메뉴';
```

메뉴 seed 예시:

```sql
INSERT INTO SMS.TB_MENU (
    MENU_ID, PARENT_MENU_ID, MENU_NM, MENU_URL,
    MENU_LEVEL, SORT_ORD, MENU_TYPE, DISPLAY_YN, USE_YN, SYSTEM_YN, REG_ID
) VALUES (
    'G_SYSTEM', NULL, '시스템관리', NULL,
    1, 40, 'G', 'Y', 'Y', 'Y', 'SYSTEM'
);

INSERT INTO SMS.TB_MENU (
    MENU_ID, PARENT_MENU_ID, MENU_NM, MENU_URL,
    MENU_LEVEL, SORT_ORD, MENU_TYPE, DISPLAY_YN, USE_YN, SYSTEM_YN, REG_ID
) VALUES (
    'SYSTEM_MENU_MANAGE', 'G_SYSTEM', '메뉴관리', '/system/menu-manage',
    2, 60, 'M', 'Y', 'Y', 'Y', 'SYSTEM'
);

INSERT INTO SMS.TB_MENU (
    MENU_ID, PARENT_MENU_ID, MENU_NM, MENU_URL,
    MENU_LEVEL, SORT_ORD, MENU_TYPE, DISPLAY_YN, USE_YN, SYSTEM_YN, REG_ID
) VALUES (
    'SYSTEM_APPROVAL_MANAGE', 'G_SYSTEM', '승인관리', '/system/approval-manage',
    2, 70, 'M', 'Y', 'Y', 'Y', 'SYSTEM'
);
```

### 4. `TB_MENU_AUTH`

역할별 메뉴 기능 권한 테이블이다.

```sql
CREATE TABLE SMS.TB_MENU_AUTH (
    MENU_ID        VARCHAR2(50) NOT NULL,
    ROLE_CD        VARCHAR2(30) NOT NULL,
    CAN_READ       CHAR(1)      DEFAULT 'N' NOT NULL,
    CAN_CREATE     CHAR(1)      DEFAULT 'N' NOT NULL,
    CAN_UPDATE     CHAR(1)      DEFAULT 'N' NOT NULL,
    CAN_DELETE     CHAR(1)      DEFAULT 'N' NOT NULL,
    CAN_APPROVE    CHAR(1)      DEFAULT 'N' NOT NULL,
    CAN_CANCEL     CHAR(1)      DEFAULT 'N' NOT NULL,
    CAN_DOWNLOAD   CHAR(1)      DEFAULT 'N' NOT NULL,
    CAN_MASK_VIEW  CHAR(1)      DEFAULT 'N' NOT NULL,
    USE_YN         CHAR(1)      DEFAULT 'Y' NOT NULL,
    REG_ID         VARCHAR2(12) NOT NULL,
    REG_DTTM       TIMESTAMP(6) DEFAULT SYSTIMESTAMP NOT NULL,
    UPD_ID         VARCHAR2(12),
    UPD_DTTM       TIMESTAMP(6),
    CONSTRAINT PK_TB_MENU_AUTH PRIMARY KEY (MENU_ID, ROLE_CD),
    CONSTRAINT FK_TB_MENU_AUTH_MENU FOREIGN KEY (MENU_ID)
        REFERENCES SMS.TB_MENU (MENU_ID),
    CONSTRAINT FK_TB_MENU_AUTH_ROLE FOREIGN KEY (ROLE_CD)
        REFERENCES SMS.TB_ROLE (ROLE_CD),
    CONSTRAINT CK_TB_MENU_AUTH_READ CHECK (CAN_READ IN ('Y', 'N')),
    CONSTRAINT CK_TB_MENU_AUTH_CREATE CHECK (CAN_CREATE IN ('Y', 'N')),
    CONSTRAINT CK_TB_MENU_AUTH_UPDATE CHECK (CAN_UPDATE IN ('Y', 'N')),
    CONSTRAINT CK_TB_MENU_AUTH_DELETE CHECK (CAN_DELETE IN ('Y', 'N')),
    CONSTRAINT CK_TB_MENU_AUTH_APPROVE CHECK (CAN_APPROVE IN ('Y', 'N')),
    CONSTRAINT CK_TB_MENU_AUTH_CANCEL CHECK (CAN_CANCEL IN ('Y', 'N')),
    CONSTRAINT CK_TB_MENU_AUTH_DOWNLOAD CHECK (CAN_DOWNLOAD IN ('Y', 'N')),
    CONSTRAINT CK_TB_MENU_AUTH_MASK CHECK (CAN_MASK_VIEW IN ('Y', 'N')),
    CONSTRAINT CK_TB_MENU_AUTH_USE_YN CHECK (USE_YN IN ('Y', 'N')),
    CONSTRAINT CK_TB_MENU_AUTH_READ_DEP CHECK (
        CAN_READ = 'Y'
        OR (
            CAN_CREATE = 'N'
            AND CAN_UPDATE = 'N'
            AND CAN_DELETE = 'N'
            AND CAN_APPROVE = 'N'
            AND CAN_CANCEL = 'N'
            AND CAN_DOWNLOAD = 'N'
            AND CAN_MASK_VIEW = 'N'
        )
    )
);

CREATE INDEX SMS.IX_TB_MENU_AUTH_01 ON SMS.TB_MENU_AUTH (ROLE_CD, USE_YN);

COMMENT ON TABLE SMS.TB_MENU_AUTH IS 'v3 역할별 메뉴 권한';
COMMENT ON COLUMN SMS.TB_MENU_AUTH.CAN_READ IS '화면 접근 및 조회 권한';
COMMENT ON COLUMN SMS.TB_MENU_AUTH.CAN_CREATE IS '신규 등록 권한';
COMMENT ON COLUMN SMS.TB_MENU_AUTH.CAN_UPDATE IS '수정 권한';
COMMENT ON COLUMN SMS.TB_MENU_AUTH.CAN_DELETE IS '삭제 권한';
COMMENT ON COLUMN SMS.TB_MENU_AUTH.CAN_APPROVE IS '승인/반려 API 접근 권한';
COMMENT ON COLUMN SMS.TB_MENU_AUTH.CAN_CANCEL IS '취소 권한';
COMMENT ON COLUMN SMS.TB_MENU_AUTH.CAN_DOWNLOAD IS '다운로드 권한';
COMMENT ON COLUMN SMS.TB_MENU_AUTH.CAN_MASK_VIEW IS '개인정보 비마스킹 조회 권한';
```

`ROLE_ADMIN` 전체 권한 seed:

```sql
INSERT INTO SMS.TB_MENU_AUTH (
    MENU_ID, ROLE_CD,
    CAN_READ, CAN_CREATE, CAN_UPDATE, CAN_DELETE,
    CAN_APPROVE, CAN_CANCEL, CAN_DOWNLOAD, CAN_MASK_VIEW,
    USE_YN, REG_ID
)
SELECT
    MENU_ID,
    'ROLE_ADMIN',
    'Y', 'Y', 'Y', 'Y',
    'Y', 'Y', 'Y', 'Y',
    'Y', 'SYSTEM'
FROM SMS.TB_MENU
WHERE USE_YN = 'Y';
```

특정 역할/메뉴 권한 upsert 예시:

```sql
MERGE INTO SMS.TB_MENU_AUTH T
USING (
    SELECT
        'SMS_HISTORY' MENU_ID,
        'ROLE_VIEWER' ROLE_CD,
        'Y' CAN_READ,
        'N' CAN_CREATE,
        'N' CAN_UPDATE,
        'N' CAN_DELETE,
        'N' CAN_APPROVE,
        'N' CAN_CANCEL,
        'Y' CAN_DOWNLOAD,
        'N' CAN_MASK_VIEW
    FROM DUAL
) S
ON (T.MENU_ID = S.MENU_ID AND T.ROLE_CD = S.ROLE_CD)
WHEN MATCHED THEN UPDATE SET
    T.CAN_READ = S.CAN_READ,
    T.CAN_CREATE = S.CAN_CREATE,
    T.CAN_UPDATE = S.CAN_UPDATE,
    T.CAN_DELETE = S.CAN_DELETE,
    T.CAN_APPROVE = S.CAN_APPROVE,
    T.CAN_CANCEL = S.CAN_CANCEL,
    T.CAN_DOWNLOAD = S.CAN_DOWNLOAD,
    T.CAN_MASK_VIEW = S.CAN_MASK_VIEW,
    T.USE_YN = 'Y',
    T.UPD_ID = 'SYSTEM',
    T.UPD_DTTM = SYSTIMESTAMP
WHEN NOT MATCHED THEN INSERT (
    MENU_ID, ROLE_CD,
    CAN_READ, CAN_CREATE, CAN_UPDATE, CAN_DELETE,
    CAN_APPROVE, CAN_CANCEL, CAN_DOWNLOAD, CAN_MASK_VIEW,
    USE_YN, REG_ID
) VALUES (
    S.MENU_ID, S.ROLE_CD,
    S.CAN_READ, S.CAN_CREATE, S.CAN_UPDATE, S.CAN_DELETE,
    S.CAN_APPROVE, S.CAN_CANCEL, S.CAN_DOWNLOAD, S.CAN_MASK_VIEW,
    'Y', 'SYSTEM'
);
```

### 5. 실행 순서

```text
1. TB_ROLE 생성
2. TB_MENU 생성
3. TB_EMP_ROLE 생성
4. TB_MENU_AUTH 생성
5. TB_ROLE 기본 역할 seed
6. TB_MENU 메뉴 seed
7. TB_EMP_ROLE 관리자 역할 부여
8. TB_MENU_AUTH ROLE_ADMIN 전체 권한 부여
9. sms.menu.source=db, sms.role.source=db 전환 후 검증
```

## 확장 판단

지금은 `TB_BUTTON_AUTH`, `TB_API_AUTH` 같은 세부 테이블을 만들지 않는다.

현재 URL suffix 정책이면 대부분의 화면 권한은 충분히 커버된다. 나중에 같은 화면 안에서 특수 버튼이 많고 버튼마다 독립 권한이 필요해지면 그때 `TB_MENU_ACTION` 같은 확장 테이블을 추가한다.

즉, 현재 단계의 목표는 다음이다.

```text
스캐폴드가 생성한 화면 URL을 TB_MENU에 등록하고,
역할별로 READ/CREATE/UPDATE/DELETE/APPROVE/CANCEL/DOWNLOAD/MASK_VIEW를 관리할 수 있게 한다.
```

## 권한관리 화면 구성안

현재 `/system/menu-manage`에서 메뉴 CRUD와 역할별 권한 매트릭스를 함께 관리한다. 역할 관리와 사용자 역할 관리는 아직 전용 화면이 없다. 필요 화면을 분리할 때는 좌측 메뉴의 `시스템관리` 그룹에 다음과 같이 둔다.

```text
시스템관리
  메뉴관리        /system/menu-manage
  역할관리        /system/role-manage
  권한관리        /system/auth-manage
  사용자역할관리  /system/emp-role-manage
```

`/system/auth-manage`는 현재 구현 URL이 아니라 향후 선택안이다. 지금은 `/system/menu-manage`에서 “메뉴 선택 → 활성 역할별 권한 체크 → 저장” 흐름을 제공한다.

### 1. 권한관리 메인 화면

URL: `/system/auth-manage`

구성:

```text
[역할 선택]
ROLE_ADMIN   시스템 관리자
ROLE_MANAGER 업무 관리자
ROLE_USER    일반 사용자
ROLE_VIEWER  조회 사용자

[권한 매트릭스]
메뉴명             READ CREATE UPDATE DELETE APPROVE CANCEL DOWNLOAD MASK_VIEW
기본메뉴           [ ]  [ ]    [ ]    [ ]    [ ]     [ ]    [ ]      [ ]
  공지사항         [x]  [x]    [x]    [x]    [ ]     [ ]    [ ]      [ ]
SMS발송조회        [ ]  [ ]    [ ]    [ ]    [ ]     [ ]    [ ]      [ ]
  발송이력조회     [x]  [ ]    [ ]    [ ]    [ ]     [ ]    [x]      [x]

[저장]
```

저장 대상은 `TB_MENU_AUTH`다. 저장 기준은 `(MENU_ID, ROLE_CD)`이며, 기존 행이 있으면 update, 없으면 insert한다.

필수 동작:

- 역할을 선택하면 해당 역할의 메뉴 권한을 조회한다.
- `READ`를 끄면 나머지 권한도 자동으로 끈다.
- `CREATE/UPDATE/DELETE/APPROVE/CANCEL/DOWNLOAD/MASK_VIEW` 중 하나를 켜면 `READ`도 자동으로 켠다.
- 그룹 메뉴(`MENU_TYPE='G'`)는 직접 저장하지 않아도 된다. 자식 중 읽을 수 있는 메뉴가 있으면 좌측 메뉴에 자동 노출한다.
- `SYSTEM_YN='Y'` 메뉴는 삭제/URL 변경 대상이 아니지만 권한 부여 대상은 될 수 있다.

### 2. 메뉴관리 탭 또는 화면

URL: `/system/menu-manage`

대상 테이블: `TB_MENU`

구성:

```text
[메뉴 트리]
기본메뉴
SMS발송조회
캠페인SMS
시스템관리

[상세]
메뉴 ID
상위 메뉴
메뉴명
URL
메뉴 타입(G/M)
정렬순서
표시 여부
사용 여부
시스템 보호 여부
```

필수 동작:

- 그룹 메뉴는 URL을 입력하지 않는다.
- 화면 메뉴는 URL을 필수로 입력한다.
- `MENU_URL`은 중복을 허용하지 않는다.
- `SYSTEM_YN='Y'`는 URL과 메뉴 ID를 수정하지 못하게 한다.
- 신규 업무 화면을 스캐폴드로 만든 뒤 이 화면에서 `TB_MENU`에 등록한다.

### 3. 역할관리 탭 또는 화면

URL: `/system/role-manage`

대상 테이블: `TB_ROLE`

구성:

```text
역할 코드     역할명        사용 여부  정렬
ROLE_ADMIN   시스템 관리자  Y         10
ROLE_MANAGER 업무 관리자    Y         20
ROLE_USER    일반 사용자    Y         30
ROLE_VIEWER  조회 사용자    Y         40
```

필수 동작:

- 역할 코드는 `ROLE_` prefix를 사용한다.
- 역할은 물리 삭제하지 않고 `USE_YN='N'`으로 비활성화한다.
- 사용 중인 역할을 비활성화할 때는 해당 역할을 가진 사용자 수를 보여준다.

### 4. 사용자역할관리 탭 또는 화면

URL: `/system/emp-role-manage`

대상 테이블: `TB_EMP_ROLE`

구성:

```text
[사용자 검색]
사번 / 이름 / 부서

[사용자 선택 결과]
EMP_ID  EMP_NM  DEP_ID  DEP_NM

[역할 체크]
[x] ROLE_ADMIN
[ ] ROLE_MANAGER
[x] ROLE_USER
```

필수 동작:

- 사용자는 `EMP + DEP` 기준으로 검색한다.
- 저장 기준은 `(EMP_ID, DEP_ID, ROLE_CD)`다.
- 사용자에게 메뉴 권한을 직접 부여하지 않는다.
- dev/prod에서 로그인하려면 사용자의 활성 역할이 최소 1개 있어야 한다.

### 5. 초기 구현 우선순위

메뉴 관리와 메뉴별 권한 매트릭스는 이미 구현되어 있다. 남은 화면은 다음 순서로 진행한다.

```text
1. 사용자역할관리 /system/emp-role-manage
   - EMP 조회
   - 역할 부여
   - TB_EMP_ROLE 저장

2. 역할관리 /system/role-manage
   - 역할 추가/비활성화

3. 선택: 권한관리 /system/auth-manage
   - 역할 중심 매트릭스가 별도로 필요할 때 현재 메뉴별 매트릭스에서 분리
```

현재 단계에서 가장 먼저 필요한 화면은 `사용자역할관리`다. 메뉴별 권한은 이미 조정할 수 있지만, 운영자가 사용자에게 역할을 부여하는 전용 흐름이 없기 때문이다.
