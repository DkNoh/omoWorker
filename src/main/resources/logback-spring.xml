<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!--
        로깅 전략
        - local/dev : 콘솔 평문(가독성 우선)
        - prod      : JSON(logstash-logback-encoder) + MDC traceId/requestId 포함
        - MDC 키: traceId(요청 추적), empId/depId(감사), menuPath
    -->
    <springProperty name="appName" source="spring.application.name" defaultValue="omo-worker"/>

    <!-- Console (local/dev) -->
    <springProfile name="local,dev,test">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%X{traceId:-}] %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- JSON (prod) -->
    <springProfile name="prod">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>traceId</includeMdcKeyName>
                <includeMdcKeyName>requestId</includeMdcKeyName>
                <includeMdcKeyName>empId</includeMdcKeyName>
                <includeMdcKeyName>depId</includeMdcKeyName>
                <includeMdcKeyName>menuPath</includeMdcKeyName>
                <includeMdcKeyName>method</includeMdcKeyName>
                <includeMdcKeyName>uri</includeMdcKeyName>
                <customFields>{"app":"${appName}"}</customFields>
                <fieldNames>
                    <timestamp>@timestamp</timestamp>
                    <version>[ignore]</version>
                </fieldNames>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>
</configuration>
