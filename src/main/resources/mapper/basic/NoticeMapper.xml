<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.scbk.sms.mapper.basic.NoticeMapper">

    <!-- Scaffold 생성(v1). 생성 후 개발자가 직접 수정해 소유한다. -->
    <!-- 화면 DatePicker 검색값은 YYYYMMDD 문자열로 전달된다 (TuiPageBuilder 규약).
         비교 컬럼이 DATE/TIMESTAMP면 TO_DATE/TO_TIMESTAMP로 감싸고,
         단일 날짜 = 조건은 당일 00:00:00 이상, 다음날 00:00:00 미만 범위로 변환한다. -->

    <sql id="searchConditions">
        <where>
        </where>
    </sql>

    <sql id="baseQuery">
            SELECT A.NOTICE_ID, A.TITLE, A.NOTICE_TYPE, A.USE_YN,
                   A.START_DT, A.END_DT, A.VIEW_CNT, A.REG_DTTM
            FROM SMS.NOTICE A
            WHERE 1=1
                <if test="searchKeyword != null and searchKeyword != ''">
                    AND A.TITLE LIKE '%' || #{searchKeyword} || '%'
                </if>
                <if test="noticeType != null and noticeType != ''">
                    AND A.NOTICE_TYPE = #{noticeType}
                </if>
                <if test="useYn != null and useYn != ''">
                    AND A.USE_YN = #{useYn}
                </if>
                <if test="startDate != null and startDate != '' and endDate != null and endDate != ''">
                    AND A.REG_DTTM BETWEEN TO_TIMESTAMP(#{startDate} || '000000', 'YYYYMMDDHH24MISS') AND TO_TIMESTAMP(#{endDate} || '235959', 'YYYYMMDDHH24MISS')
                </if>
    </sql>

    <select id="count" resultType="int">
        /* NoticeMapper.count */
        SELECT COUNT(1) FROM (
        <include refid="baseQuery"/>
        ) A
        <include refid="searchConditions"/>
    </select>

    <select id="selectList" resultType="com.scbk.sms.vo.basic.NoticeVO">
        /* NoticeMapper.selectList */
        SELECT A.*
        FROM (
        <include refid="baseQuery"/>
        ) A
        <include refid="searchConditions"/>
        ORDER BY A.NOTICE_ID
        OFFSET #{offset} ROWS FETCH NEXT #{size} ROWS ONLY
    </select>

</mapper>
